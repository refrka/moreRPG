class_name itemEnums extends Node

enum ItemType {
	NULL,
	CRAFTING,
	EQUIPMENT,
	POTION,
	PROVISION,
	TOOL,
	VALUABLE,
	WEAPON,
}

enum NullItem {
	NULL,
}

enum CraftingItem {
	NULL,
	CLOTH_STRIP,
}

enum EquipmentItem {
	NULL,
	BOOTS_LEATHER,
}

enum PotionItem {
	NULL,
	LIGHT_HEALING,
}

enum ProvisionItem {
	NULL,
	WILD_BERRIES,
}

enum ToolItem {
	NULL,
	TORCH,
}

enum ValuableItem {
	NULL,
	ROUGH_GEM,
}

enum WeaponItem {
	NULL,
	DAGGER_IRON,
}